# Final Comparison: DL Project vs. Internship

> Draft — numbers below are placeholders until `notebooks/04_cnn_lstm.ipynb` and
> `notebooks/06_interpretability.ipynb` finish executing. Structure is final; this
> note is removed once real numbers are filled in.

## 1. What changed, in one paragraph

The internship trained two independent single-target regressors (Ridge for DBT,
LightGBM for WBT) on a flat 13-feature table, selected by a random 80:20 split.
This project trains one multi-task CNN-LSTM (+ lightweight self-attention) that
predicts DBT and WBT jointly from a (4-timestep x 10-channel) sequence built from
the same underlying variables, evaluated on a stricter chronological, burst-aware
split — and benchmarks it honestly against classical baselines refit on that same
harder split, not just against the internship's original numbers.

## 2. Headline numbers

| Model | Split | Target | R2 | RMSE | MAE | KGE |
|---|---|---|---|---|---|---|
| Internship Ridge (random split) | test | DBT | 0.6213 | 1.2064 | 0.8792 | 0.7815 |
| Internship LightGBM (random split) | test | WBT | 0.7092 | 0.7372 | 0.5442 | 0.7474 |
| Best classical, this split | test | DBT | TBD | TBD | TBD | TBD |
| Best classical, this split | test | WBT | TBD | TBD | TBD | TBD |
| CNN-LSTM hybrid (default) | test | DBT | TBD | TBD | TBD | TBD |
| CNN-LSTM hybrid (default) | test | WBT | TBD | TBD | TBD | TBD |
| CNN-LSTM hybrid (tuned) | test | DBT | TBD | TBD | TBD | TBD |
| CNN-LSTM hybrid (tuned) | test | WBT | TBD | TBD | TBD | TBD |

(Full table: `data/processed/final_comparison_table.csv`, generated by notebook 04.)

## 3. Does the DL model beat classical ML here? (honest answer, not assumed)

TBD — filled in from the table above. At n=210 training rows, there is a real
possibility the classical baselines (refit on this project's own split) win or tie;
the brief explicitly asked for this to be reported honestly either way, since a
"DL doesn't help at n=300" finding is itself a valid, discussable result, not a
failure of the project.

## 4. Ablation: what does each architectural piece actually buy?

From notebook 04's LSTM-only vs CNN-only vs CNN-LSTM-hybrid comparison — TBD.

## 5. Optimizer comparison

Adam vs SGD vs RMsprop on the identical CNN-LSTM architecture — TBD (convergence
speed, final val loss, test metrics).

## 6. Overfitting check: train-vs-test gap

From notebook 04's `gap_df` — TBD per model/target.

## 7. Interpretability comparison

Internship SHAP (Section 3.4): DBT dominated by `T(i-1)`, then `Hr`, then surface
latent heat flux; WBT dominated by `T(i-4)`, then `T(i-1)`, OLR, `T(i-2)`.

This project's CNN-LSTM SHAP ranking (notebook 06, `KernelExplainer` on the
flattened sequence, channel-summed across timesteps) — TBD. Key question answered
directly from the numbers: does the lag-temperature channel dominate the static
meteorological channels here too, consistent with the internship's finding that
temporal persistence dominates both targets?

## 8. Where DL helped, where it didn't — the honest takeaway

TBD, written after all of the above is in. Will state plainly if the CNN-LSTM
underperforms Ridge/LightGBM at this sample size, and why that is still a
legitimate, informative result for a portfolio project (small-n behavior of DL
vs. classical ML is itself a real, defensible finding — not a failure to hide).

## 9. Bottom line for a reader deciding what this project demonstrates

TBD.
